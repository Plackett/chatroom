#ifndef LOBBY_H
#define LOBBY_H

// Lobby.h
// This class provides the main menu for the user.
// It allows them to choose between creating a new chat room or joining an existing one.
class Lobby {
public:
    // Displays the main lobby menu and handles user input.
    static void show();
};

#endif // LOBBY_H
